{
  "version": 2,
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/angular",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist/twitter-clone"
      }
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist/twitter-clone"
}
